:root {
  --bg: #f5f6f8;
  --card: #fff;
  --text: #222;
  --accent: #2b7;
}
* { box-sizing: border-box; }
body { margin: 0; font-family: system-ui, -apple-system, "Segoe UI", Roboto; background: var(--bg); color: var(--text); }
.container { max-width: 900px; margin: 40px auto; padding: 12px; }
header { text-align: center; margin-bottom: 8px; }
h1 { font-size: 1.4rem; }

.form { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; align-items: start;
  padding: 12px; border-radius: 8px; background: #fff; box-shadow: 0 2px 6px rgba(0,0,0,.05); }
.form label { display:flex; flex-direction:column; gap:6px; font-size: 0.92rem; }
.form input, .form select { padding: 8px; border:1px solid #ddd; border-radius:6px; }
.form .row { display: flex; align-items: center; gap: 6px; }
button { grid-column: span 2; justify-self: start; padding: 10px 14px; border: none; background: #007bff; color:white; border-radius:6px; cursor: pointer; }
button:hover { background: #0056b3; }

.downloads { margin-top: 18px; padding:12px; border-radius:8px; background:#fff; border:1px solid #eee; }
.btns button { margin-right:8px; padding:8px 12px; border-radius:6px; border:1px solid #ddd; background:#f8f8f8; cursor:pointer; }
.note { font-size: 0.85rem; color:#555; margin-top:8px; }
